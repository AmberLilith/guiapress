<%- include ("partials/header.ejs") %>

    <body>
        <%- include ("partials/homenavbar.ejs") %>
            <div class="container">
                <hr>
                <div class="jumbotron">
                    <h1 class="display-4">bem-vindo</h1>
                    <p class="lead">Blog guia de programação / GUIAPRESS</p>
                    <hr class="my-4">
                    <a href="#" class="btn btn-primary btn-lg">Leia mais</a>                    
                </div>
                <hr>
                <% articles.forEach(article => {%>
                    <div class="card">
                        <div class="card-header">
                            <h2><%= article.title %></h2>
                        </div>
                        <div class="card-body">
                            <a href="/home/<%= article.slug %>" class="btn btn-success">Ler artigo</a>
                        </div>
                    </div>
                    <br>
                <%})%>
                
            </div>
    </body>

    <%- include ("partials/footer.ejs") %>
        